# תוכנית ליטוש דמו — לפי ניתוח UX 6.7.2026

> מבוסס על ניתוח הדמו שאושר ע"י מור ("הכן תוכנית לתיקון הכל, בסדר שהמלצת. ובצע").
> commit אחרי כל משימה, פריסה demo-v3 בסוף + test-remote.

## P1 — באג ManualLoginForm (קריטי)
הטופס הידני מנווט בלי לעדכן את מערכת הקול.
תיקון: ב-`handleSubmit` — sessionStorage + `sadan:authenticated` + `sadan_skip_greeting`,
בדיוק כמו `doLogin()` של המסלול הקולי.
**בדיקה:** כניסה דרך הטופס → סדן לא מבקש הזדהות.

## P2 — PTT למצב קהל + חיווי "מקשיב" ברור
- P2.1: דגל `sadan_ptt_default` ב-localStorage + מתג ב-DemoControlPanel ("מצב קהל — PTT").
  בהתחברות קולית, אם הדגל דלוק → מצב PTT אוטומטית.
- P2.2: התווית מתחת ל-orb תציג מצב אמיתי: "מקשיב" (ירוק) / "סדן מדבר" (זהב) / "סדן" (כבוי) —
  כך ברור למשתמש מתי המיקרופון באמת פתוח (ה-orb כבר מדויק כי speaking כולל את חלון ההשתקה).
**בדיקה:** מתג בפאנל עובד; התווית משתנה בזמן שיחה.

## P3 — מסמך אימות לרז
`docs/raz_review_screen_guide.md`: כל הטקסטים שסדן אומר על מסכים (SCREEN_GUIDE),
+ רשימת בדיקות עקביות סיפור (יעדים/כוחות/בטונדות בין מסכים).
**לא נשלח לרז — מוצג למור.**

## P4 — חוק 16:00 (רטג) נשלט מפאנל הדמו
דגל `sadan_ignore_rtg_hours` + מתג ב-DemoControlPanel. ברירת מחדל: החוק פעיל (פיצ'ר ריאליזם),
אבל בדמו אחה"צ — כיבוי בלחיצה במקום מוקש.
**בדיקה:** מתג מכבה את הבאנר.

## P5 — מסגור: כאב בפתיחה + חיסכון בסיום
- P5.1: שורת מסגור במסך הכניסה: "מתכנון של שבועות — לאישור מוכן לביצוע בדקות".
- P5.2: אחרי ה-GO במסך אישורים: שורת סיכום — "N גורמי תיאום • תיק תרגיל מלא • מוכן לביצוע"
  (N מחושב מהנתונים, לא מומצא).
**בדיקה:** ויזואלית.

## סיום
build → commit-ים → פריסה `demo-v3` → `test-remote.ps1` → V1 חוזר למור.
